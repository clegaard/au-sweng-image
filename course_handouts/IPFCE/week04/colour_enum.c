/*
 * Compute the factorial of a given number.
 */

#include <stdio.h>  /* scanf, printf */
#include <assert.h> /* assert */

typedef enum { RED, GREEN , BLUE } colour;
typedef enum { BLACK, WHITE } shade;

/* program body */
int main(void)
{
 	shade c;

    /* print output */
    printf("%d\n", RED);
    printf("%d\n", GREEN);
    printf("%d\n", BLUE);
    
    c = RED;
    
    /* return from body and exit program */
    return 0;
}